@model IEnumerable<Quiz.Models.DapAn>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IsTrue)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NoiDung)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IsTrue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NoiDung)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DapAnId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DapAnId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DapAnId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>