@model IEnumerable<Quiz.Models.Nhom>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ten)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ten)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NhomId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NhomId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NhomId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>